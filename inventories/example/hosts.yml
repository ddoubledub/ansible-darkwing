---
# Example inventory — copy this directory and rename it (e.g. "prod", "staging")
# then populate with your real hosts and variables.

all:
  children:
    webservers:
      hosts:
        web-server-01:
          ansible_host: 192.168.1.10
    dbservers:
      hosts:
        db-server-01:
          ansible_host: 192.168.1.20
